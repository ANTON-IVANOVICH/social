# Адреса NestJS-бэкенда (apps/api). CORS в dev рефлексирует origin + credentials
# (нужно для httpOnly refresh-cookie). Авторизация — через auth-link + refresh-ротацию.
VITE_API_URL=http://localhost:3000/graphql
VITE_WS_URL=ws://localhost:3000/graphql
